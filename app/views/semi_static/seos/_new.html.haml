.button= link_to_function 'Cancel', "$('#dialog').hide();"
%h1= ['New SEO meta tags for ', @seo.seoable.raw_title, ' (', @seo.seoable.class.to_s.split('::').last, ')'].join
%h3= ['locale:', @seo.seoable.locale.to_s].join(' ')
= render 'form'
