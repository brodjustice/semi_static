.button= link_to 'Cancel', '#', :onclick => "$('#dialog').hide(); return false;"
%h1= ['New hreflang tag for ', @hreflang.seo.seoable.raw_title, ' (', @hreflang.seo.seoable.class.to_s.split('::').last, ')'].join
= render 'form'
