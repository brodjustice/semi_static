- if @tag.banner
  - content_for :banner do
    #overlay-2
      - unless @tag.effective_tag_line.blank?
        #tag-wrapper
          #tagline
            = @tag.effective_tag_line
    #banner
      #banner-frame
        .bg.desktop{:style => "background-image: url(#{@tag.banner.img_url_for_theme(:desktop)})"}
        .bg.mobile{:style => "display: none; background-image: url(#{@tag.banner.img_url_for_theme(:mobile)})"}


-# TODO: Refactor
- if @tag.partial_before_entries?
  = render :partial => @tag.partial_path
- if @tag.show_entries?
  .section
    - if @tag.side_bar
      .col.span_2_of_3.main-col
        = render 'main_col'
      .col.span_1_of_3.side-bar
        = render 'side_bar'
    - else
      .col.span_3_of_3.main-col
        = render 'main_col'
- if @tag.partial_after_entries?
  = render :partial => @tag.partial_path
