- url = e.summary.split(' ').first || @newsletter.website_url
%tr
  %td.nl_entry{:colspan => '2'}
    = render :partial => 'controls', :locals => {:e => e}
    = link_to url do
      .img{:style => 'text-align:center;margin-bottom:10px;', :id => "entry_id_#{e.id}"}
        = render :partial => 'image', :locals => {:e => e}
