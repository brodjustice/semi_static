-# This partial is paired with the col_span_1_of_2_wrapper partial and should come before that as a merged entry. It
-# will them act as a side-box like div which will transition to full with on slim screens (like mobiles).

- content_for :header_css do
  :plain
    .floater{width: 49.2% !important;}
    @media only screen and (max-width: 760px) { .floater{ width: auto !important; } }


.col.span_1_of_2.floater{:class => e.style_class, :style => "background-color:#{e.background_colour}; color:#{e.colour}"}
  %h2{:style => "color: #{e.header_colour};"}= e.title
  %h3{:style => "color: #{e.header_colour};"}= e.sub_title
  - if !e.image_disable && e.img.present?
    = render :partial => 'semi_static/entries/image', :locals => {:e => e, :style => style}
  = entry_body(e)
  = photo_main(e, (@group_size || 2) - 1)

