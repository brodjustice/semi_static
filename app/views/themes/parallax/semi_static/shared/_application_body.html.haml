#slide-menu
  = render 'semi_static/shared/slide_menu'
#top
  #top-wrapper
    #logo-wrapper
      = link_to semi_static.root_path do
        #logo
          = SemiStatic::Engine.config.site_name
    = yield :pre_overlay
    #overlay
      #overlay-inner
        = render 'semi_static/shared/menu'
= yield :banner
= yield :pre_content
#frame
  #container
    #dialog{:style => 'display: none;'}
    #content
      =yield
  = yield :post_content
