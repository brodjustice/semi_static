Description:
    Copies the SemiStatic database migrations, seed data and other useful files to your application ready to run rake db:setup

Example:
    rails generate semi_static_install

    This will create:
        db/seed.rb
        db/migrations/*
